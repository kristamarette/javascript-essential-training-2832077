const cup = {
    name : "a cup of coffee",
    color : '#fff',
    pattern : null,
    handle : true,
    temperature : "hot",
    contains : {
        coffee : 80,
        almondMilk : 20,
    }
}

console.log("My favorite morning beverage is:", cup );
console.log(cup.name);
console.log("The first thing I make in the morning", cup.name);